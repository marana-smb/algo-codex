# notebook-utils (Python 3.7)

Install (editable) into your conda env:

    pip install -e /path/to/notebook_utils

Then in any notebook:

    import _formatting_functions
    import _ts_functions

Edits to modules are picked up after kernel restart or importlib.reload.
