[metadata]
name = notebook-utils
version = 0.1.0
description = Shared utility modules for Jupyter notebooks (editable install)
license = Proprietary

[options]
python_requires = >=3.7
package_dir =
    = src
packages = find:

[options.packages.find]
where = src